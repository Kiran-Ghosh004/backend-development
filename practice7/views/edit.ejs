<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit File Name</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-gradient-to-br from-zinc-900 via-zinc-800 to-zinc-900 flex flex-col items-center justify-center min-h-screen text-white p-4">

  <!-- Page Title -->
  <h1 class="text-4xl font-bold text-blue-400 mb-8 uppercase tracking-wide drop-shadow-lg">
    Rename: <%= title %>
  </h1>

  <!-- Edit Box -->
  <div class="max-w-md w-full bg-zinc-800/70 backdrop-blur-xl rounded-3xl shadow-2xl border border-zinc-700 p-8">
    <form action="/edit" method="POST" class="flex flex-col space-y-5">

      <!-- Hidden old filename -->
      <input type="hidden" name="oldtitle" value="<%= title %>">

      <!-- Input for new filename -->
      <label for="title" class="text-gray-300 text-sm font-semibold">Enter New File Name</label>
      <input 
        type="text" 
        id="title" 
        name="title"
        placeholder="New file name (without .txt)" 
        required
        class="w-full px-4 py-2 rounded-lg border border-zinc-700 bg-zinc-900/70 focus:ring-2 focus:ring-blue-500 outline-none text-gray-200 placeholder-gray-500 transition"
      >

      <!-- Save Button -->
      <button 
        type="submit"
        class="w-full py-2 rounded-lg bg-yellow-600 hover:bg-blue-700 active:scale-95 transition-all duration-200 font-semibold tracking-wide">
        Save Changes
      </button>
    </form>
  </div>

</body>
</html>
