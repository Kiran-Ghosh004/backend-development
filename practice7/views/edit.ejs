<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Note</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-gradient-to-br from-zinc-900 via-zinc-800 to-zinc-900 flex flex-col items-center justify-center min-h-screen text-white p-4">

  <h1 class="text-4xl font-bold text-blue-400 mb-6 uppercase tracking-wide drop-shadow-lg">
    Edit: <%= title %>
  </h1>

  <div class="max-w-2xl w-full bg-zinc-800/70 backdrop-blur-xl rounded-3xl shadow-2xl border border-zinc-700 p-8">
    <form action="/edit/<%= title %>" method="POST" class="flex flex-col space-y-4">
      <textarea name="details" class="w-full px-4 py-2 h-60 rounded-lg border border-zinc-700 bg-zinc-900/70 focus:ring-2 focus:ring-blue-500 outline-none text-gray-200 resize-none transition"><%= details %></textarea>

      <button type="submit" class="w-full py-2 rounded-lg bg-blue-600 hover:bg-blue-700 active:scale-95 transition-all duration-200 font-semibold tracking-wide">
        Save Changes
      </button>
    </form>
  </div>

</body>

</html>
