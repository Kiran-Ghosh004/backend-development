<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-zinc-900 text-white min-h-screen">

    <div class="w-full bg-zinc-800 p-4 flex justify-between items-center shadow-lg">
        <h1 class="text-xl font-semibold">All User Posts</h1>
        <a href="/profile" class="px-4 py-2 bg-blue-600 hover:bg-blue-700 rounded-lg text-sm transition">
            Back to Profile
        </a>
    </div>

    <div class="max-w-3xl mx-auto mt-10 space-y-6">

        <% posts.forEach(post => { %>
            <div class="bg-zinc-800 p-6 rounded-2xl shadow-lg">

                <h4 class="text-gray-300 text-sm">
                    Posted by:
                    <span class="text-white font-semibold"><%= post.user.username %></span>
                </h4>

                <h3 class="text-xl font-bold mt-2"><%= post.title %></h3>

                <p class="text-gray-300 mt-2"><%= post.content %></p>

                <a href="/like/<%= post._id %>" 
   class="mt-4 inline-block text-blue-400 hover:underline text-sm">
    ❤️ <%= post.likes?.length || 0 %> Likes
</a>


            </div>
        <% }) %>

    </div>

</body>
</html>
