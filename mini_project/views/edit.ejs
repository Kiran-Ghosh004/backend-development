<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center">

    <div class="bg-zinc-800 p-8 rounded-2xl w-full max-w-lg shadow-lg">
        <h2 class="text-2xl font-bold mb-5">Edit Your Post</h2>

        <form action="/edit/<%= post._id %>" method="post" class="space-y-4">

            <input type="text" name="title" value="<%= post.title %>"
                class="w-full px-4 py-2 bg-zinc-700 rounded-lg text-white focus:ring-2 focus:ring-blue-500">

            <textarea name="content" rows="4"
                class="w-full px-4 py-2 bg-zinc-700 rounded-lg text-white focus:ring-2 focus:ring-blue-500"><%= post.content %></textarea>

            <button class="w-full py-2 bg-blue-600 hover:bg-blue-700 rounded-lg">Update</button>
        </form>

        <a href="/profile" class="block text-center text-gray-400 mt-4 hover:underline">
            Cancel
        </a>
    </div>

</body>
</html>
